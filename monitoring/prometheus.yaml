# monitoring/prometheus.yaml
#
# Scrapes the OpenTelemetry collector's Prometheus exporter, which carries
# nostream's relay metrics.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets:
          - otel-collector:8889